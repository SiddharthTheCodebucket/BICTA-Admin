import React from 'react';
import { CommsSupportFormScreen } from './CommsSupportShared';

const AddSubCategory = (props: any) => (
  <CommsSupportFormScreen {...props} type="subCategory" />
);

export default AddSubCategory;
